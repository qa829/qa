# os: osx
# language: generic
# script:
# - brew info
# - brew update
# - brew info

# language: bash
# arch:
#   - amd64
#   - arm64
# install: skip
# script: skip
# cache:
#   directories:
#     - foo
   
language: minimal
python:
  - "2.7"
install: skip
script:
- python -V
#- python ./src/hello.py
matrix:
  include:
    - arch: arm64
      os: linux
    - arch: amd64
      os: linux
    - arch: arm64
      os: linux
      dist: bionic
    - arch: amd64
      os: linux
      dist: bionic
